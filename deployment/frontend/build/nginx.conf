 
worker_processes 5;

events { worker_connections 1024; }

http {
    include    mime.types;
    sendfile on;
    server {

listen 80;
listen [::]:80;
 
   root /usr/share/nginx/html;

location /
{
	 
       try_files $uri /index.html /index.js;
       }
}
}
